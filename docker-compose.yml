version: '3.9'

services:
  path_db:
    image: postgres:alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: simcha
      POSTGRES_PASSWORD: appled^ice
      POSTGRES_DB: cipheRreportDb
    volumes:
      - pgdata:/var/lib/postgresql/data
    ports:
      - "5433:5432"


volumes:
  pgdata: